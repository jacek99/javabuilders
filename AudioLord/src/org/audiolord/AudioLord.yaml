Window(title=AudioLord):
    - VBox(homogenous=false):
        - MenuBar(name=menuBar):
            - MenuItem(name=file,label=File):
                 - Menu(name=fileMenu):
                     - MenuItem(label=Save)
        - Button(label=Test)
